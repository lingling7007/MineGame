using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MakeCastleHelper
{

    /// <summary>
    /// 生成地图城堡信息
    /// </summary>
    public static List<Vector3> InitialCastle(int worldWidth, int worldLength, int count)
    {
        List<Vector3> list = new List<Vector3>();


        return list;
    }

    public static void CreateCastle(List<Vector3> list, int worldWidth, int worldLength, int count)
    {
        int x = Random.Range(0, worldWidth);
        int y = Random.Range(0, worldLength);


    }
}
